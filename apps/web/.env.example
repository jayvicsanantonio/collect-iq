# AWS Configuration
NEXT_PUBLIC_REGION=us-east-1

# Amazon Cognito Configuration
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_COGNITO_DOMAIN=collectiq.auth.us-east-1.amazoncognito.com

# OAuth Configuration
NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
NEXT_PUBLIC_OAUTH_LOGOUT_URI=http://localhost:3000

# API Configuration
NEXT_PUBLIC_API_BASE=https://api.collectiq.com

# Optional: Feature Flags (comma-separated)
# Available flags: pricing, authenticity, charts, camera_capture, vault_filters, debug
# Example: FEATURE_FLAGS=pricing,authenticity,charts
FEATURE_FLAGS=

# Optional: Analytics
# Set to enable analytics tracking (respects Do Not Track)
NEXT_PUBLIC_ANALYTICS_ID=

# Optional: Error Tracking
SENTRY_DSN=
